<%- model_class = ManageUser -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:user_id) %>:</strong></dt>
  <dd><%= @manage_user.user_id %></dd>
  <dt><strong><%= model_class.human_attribute_name(:rol_id) %>:</strong></dt>
  <dd><%= @manage_user.rol_id %></dd>
</dl>

<%= link_to t('.back', :default => t("helpers.links.back")),
              manage_users_path, :class => 'btn btn-default'  %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_manage_user_path(@manage_user), :class => 'btn btn-default' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              manage_user_path(@manage_user),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
